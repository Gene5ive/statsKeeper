<h1 class="page-header text-center">New Player</h1>

<form>
  <div class="form-group">
    <label for="player-name">Player's Name: </label>
    {{input value=player-name id="player-name" class="form-control"}}
  </div>

  <button class="btn btn-primary" {{action "save"}}>Add Player</button>
</form>

{{outlet}}
